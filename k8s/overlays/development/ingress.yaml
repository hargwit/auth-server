apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    name: auth-ingress
spec:
    rules:
        - host: localhost
          http:
              paths:
                  - path: /
                    pathType: Prefix
                    backend:
                        service:
                            name: auth-server
                            port:
                                number: 80
